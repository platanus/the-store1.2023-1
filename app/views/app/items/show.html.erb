<div class="flex flex-col content-start gap-12">
  <a
    href="/"
    class="flex flex-row justify-center py-2 px-4 gap-1 items-center bg-zinc-100 rounded-full max-w-min text-zinc-800 text-sm"
  >
    <%= image_tag("back-arrow.svg") %>
    Volver
  </a>
  <item-show-detail :item="<%= serialize_resource(@item) %>">
  </item-show-detail>
  <item-show-review
    :item="<%= serialize_resource(@item) %>"
    :reviews="<%= serialize_resource(@reviews) %>"
  >
  </item-show-review>
</div>
